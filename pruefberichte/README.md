# Prüfberichte

Hier legt der Prüfer unabhängige Prüfberichte zu Reformvorhaben ab.

Namensschema:

```text
YYYY-MM-DD-pruefung-<slug>.md
```

Ein Prüfbericht muss den geprüften Report verlinken und genau einen Status
verwenden: `freigegeben`, `blockiert` oder `offen`.
